'use client';

export function TestModeBanner() {
  return (
    <div className="bg-[var(--il-gummy)] text-[var(--il-ink)] text-center py-2 px-4 text-sm font-medium">
      🧪 TEST MODE — Stripe test mode. No real charges will be made.
    </div>
  );
}
